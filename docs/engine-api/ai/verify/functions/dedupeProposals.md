# Function: dedupeProposals()

> **dedupeProposals**(`proposals`, `currentModel`): [`Proposal`](../../types/interfaces/Proposal.md)[]

Defined in: ai/verify.ts:68

Drop no-op proposals (identical to current) and de-duplicate by layout.

## Parameters

### proposals

[`Proposal`](../../types/interfaces/Proposal.md)[]

### currentModel

[`Model`](../../../model/types/interfaces/Model.md)

## Returns

[`Proposal`](../../types/interfaces/Proposal.md)[]
