# Variable: VISION\_TEXT

> `const` **VISION\_TEXT**: `"This is a routing sheet or a hand-drawn cell layout. Extract a FlowPlan model. Respond with model JSON only (name, gridW, gridH, stations[], flows[])."` = `"This is a routing sheet or a hand-drawn cell layout. Extract a FlowPlan model. Respond with model JSON only (name, gridW, gridH, stations[], flows[])."`

Defined in: ai/prompts.ts:42
