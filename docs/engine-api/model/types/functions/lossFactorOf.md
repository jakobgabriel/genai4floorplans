# Function: lossFactorOf()

> **lossFactorOf**(`model`): `number`

Defined in: model/types.ts:525

A model's loss factor, clamped to sane bounds, defaulting when unset.

## Parameters

### model

#### lossFactor?

`number`

## Returns

`number`
