# Function: partsPerCycleOf()

> **partsPerCycleOf**(`s`): `number`

Defined in: model/types.ts:458

Parts processed together in one cycle (≥1). A multi-part step outputs this
 many parts per cycle, so its per-part time is the cycle divided by it.

## Parameters

### s

`Pick`\<[`Station`](../interfaces/Station.md), `"partsPerCycle"`\>

## Returns

`number`
