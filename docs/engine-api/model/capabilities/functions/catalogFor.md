# Function: catalogFor()

> **catalogFor**(`model`): [`Capability`](../interfaces/Capability.md)[]

Defined in: model/capabilities.ts:52

The catalog to use for a model: its own governed list if it carries one,
 otherwise the seeded default. Keeps offline/first-launch working (§1.1).

## Parameters

### model

#### capabilities?

[`Capability`](../interfaces/Capability.md)[]

## Returns

[`Capability`](../interfaces/Capability.md)[]
