# Function: newStationId()

> **newStationId**(`model`, `base?`): `string`

Defined in: store/reducer.ts:302

A fresh station id that is unique within the model.

## Parameters

### model

[`Model`](../../../model/types/interfaces/Model.md)

### base?

`string` = `"step"`

## Returns

`string`
