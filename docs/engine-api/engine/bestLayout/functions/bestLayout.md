# Function: bestLayout()

> **bestLayout**(`model`, `opts?`): [`BestLayout`](../interfaces/BestLayout.md)

Defined in: engine/bestLayout.ts:39

Lowest-flow-cost layout reachable by repositioning the movable stations —
the cheapest of a pairwise optimize and each cell form. Deterministic.

## Parameters

### model

[`Model`](../../../model/types/interfaces/Model.md)

### opts?

[`OptimizeOptions`](../../optimize/interfaces/OptimizeOptions.md) = `{}`

## Returns

[`BestLayout`](../interfaces/BestLayout.md)
