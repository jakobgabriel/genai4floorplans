# Function: normalizeWeights()

> **normalizeWeights**(`w`): [`RatingWeights`](../../../model/types/interfaces/RatingWeights.md)

Defined in: engine/rating.ts:21

Normalize weights so they sum to 1 (UI lets users drag them arbitrarily).

## Parameters

### w

[`RatingWeights`](../../../model/types/interfaces/RatingWeights.md)

## Returns

[`RatingWeights`](../../../model/types/interfaces/RatingWeights.md)
