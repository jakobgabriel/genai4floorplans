# Function: isValueAdd()

> **isValueAdd**(`key`): `boolean`

Defined in: engine/cycle.ts:31

Only valueAddSec adds value; the rest is waste by definition.

## Parameters

### key

`"valueAddSec"` \| `"handlingSec"` \| `"walkSec"` \| `"waitSec"` \| `"setupSec"`

## Returns

`boolean`
