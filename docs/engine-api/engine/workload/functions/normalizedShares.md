# Function: normalizedShares()

> **normalizedShares**(`modes`): `number`[]

Defined in: engine/workload.ts:33

Shares renormalised to sum to 1, so a mis-entered mix can't skew the maths.

## Parameters

### modes

[`VariantMode`](../../../model/types/interfaces/VariantMode.md)[]

## Returns

`number`[]
