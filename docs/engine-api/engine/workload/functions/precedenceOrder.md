# Function: precedenceOrder()

> **precedenceOrder**(`elements`): `string`[] \| `null`

Defined in: engine/workload.ts:261

Topological order over the precedence DAG; returns null if it has a cycle.

## Parameters

### elements

[`WorkElement`](../../../model/types/interfaces/WorkElement.md)[]

## Returns

`string`[] \| `null`
